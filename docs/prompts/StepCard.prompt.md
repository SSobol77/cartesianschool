Numbered process step — the "How it works" grid (Free Audit → Live Preview → Fixed Price → 5-Day Launch).

```jsx
<StepCard n="01" icon={<Search />} title="Free Audit" desc="We run Google's own tests…" />
```

Gradient icon tile + ghosted mono step number. Composes `Card`.
