Feature highlight with an icon tile, title and one-line description. Replaces the live site's emoji tiles with clean line icons.

```jsx
<FeatureCard icon={<Smartphone />} title="Mobile-First" desc="Built for phones first." />
```

`layout="row"` (icon left) or `"stack"` (icon above). Composes `Card`.
